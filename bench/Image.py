"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class Image(object):

    __slots__ = ["height", "width", "encoding", "step", "data_len", "data"]

    __typenames__ = ["int32_t", "int32_t", "string", "int32_t", "int32_t", "byte"]

    __dimensions__ = [None, None, None, None, None, ["data_len"]]

    def __init__(self):
        self.height = 0
        """ LCM Type: int32_t """
        self.width = 0
        """ LCM Type: int32_t """
        self.encoding = ""
        """ LCM Type: string """
        self.step = 0
        """ LCM Type: int32_t """
        self.data_len = 0
        """ LCM Type: int32_t """
        self.data = b""
        """ LCM Type: byte[data_len] """

    def encode(self):
        buf = BytesIO()
        buf.write(Image._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">ii", self.height, self.width))
        __encoding_encoded = self.encoding.encode('utf-8')
        buf.write(struct.pack('>I', len(__encoding_encoded)+1))
        buf.write(__encoding_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">ii", self.step, self.data_len))
        buf.write(bytearray(self.data[:self.data_len]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != Image._get_packed_fingerprint():
            raise ValueError("Decode error")
        return Image._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = Image()
        self.height, self.width = struct.unpack(">ii", buf.read(8))
        __encoding_len = struct.unpack('>I', buf.read(4))[0]
        self.encoding = buf.read(__encoding_len)[:-1].decode('utf-8', 'replace')
        self.step, self.data_len = struct.unpack(">ii", buf.read(8))
        self.data = buf.read(self.data_len)
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if Image in parents: return 0
        tmphash = (0xfdf12616ab556c02) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if Image._packed_fingerprint is None:
            Image._packed_fingerprint = struct.pack(">Q", Image._get_hash_recursive([]))
        return Image._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", Image._get_packed_fingerprint())[0]

